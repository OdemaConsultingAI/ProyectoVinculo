# EAS Build: excluir archivos que no necesita el build
# Evita subir docs y archivos que puedan causar problemas en el servidor (tar en Linux)
.git/
node_modules/
.expo/
dist/
web-build/
*.log
.env*.local
# IDE y sistema (evitar rutas o caracteres que rompan tar)
.cursor/
.cursorignore
.vscode/
.idea/
.DS_Store
Thumbs.db
# Docs (opcional, reduce tamaño)
*.md
